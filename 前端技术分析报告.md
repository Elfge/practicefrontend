# 408考研题库系统前端详细技术报告

## 一、项目概述

这是一个基于 Vue 3 的 408 考研题库学习系统，为计算机考研学生提供完整的题库练习、模拟考试、错题管理和学习数据分析功能。

---

## 二、技术栈详解

### 2.1 核心框架层

#### **Vue 3.2.13** - 渐进式 JavaScript 框架

**技术原理：**
- **响应式系统**：基于 ES6 Proxy 实现，相比 Vue 2 的 Object.defineProperty 有更好的性能和更全面的拦截能力
- **Composition API**：通过 `setup()` 函数、`ref`、`reactive`、`computed` 等组合式 API，实现更灵活的逻辑复用和代码组织
- **虚拟 DOM**：通过 diff 算法最小化 DOM 操作，提升渲染性能

**在本项目中的应用：**
```javascript
// Home.vue 中的响应式数据定义
const stats = ref({
  totalQuestions: 0,
  completedQuestions: 0,
  correctRate: 0,
  studyDays: 0
})

// 计算属性 - 自动缓存，依赖变化时重新计算
const currentQuestion = computed(() => {
  return questions.value[currentIndex.value] || null
})
```

#### **Vue Router 4.6.4** - 官方路由管理器

**技术原理：**
- 基于 HTML5 History API 或 Hash 模式实现单页面应用（SPA）路由
- 支持路由懒加载（动态导入），优化首屏加载速度
- 提供导航守卫（beforeEach、beforeResolve）实现权限控制

**在本项目中的应用（router/index.js）：**
```javascript
const router = createRouter({
  history: createWebHistory(),  // 使用 HTML5 History 模式
  routes
})

// 全局前置守卫 - 实现身份验证和权限控制
router.beforeEach((to, from, next) => {
  const token = localStorage.getItem('token')
  const userStr = localStorage.getItem('user')
  const user = userStr ? JSON.parse(userStr) : null

  if (to.meta.requiresAuth && !token) {
    next('/login')  // 未登录重定向
  } else if (to.meta.requiresAdmin && user?.role !== 'ADMIN') {
    next('/home')   // 非管理员重定向
  } else {
    next()
  }
})
```

**路由懒加载实现：**
```javascript
component: () => import('@/views/PracticeMode.vue')  // webpack 代码分割
```

#### **Vuex 4.0.2** - 状态管理模式

**技术原理：**
- 采用集中式存储管理应用的所有组件状态
- 核心概念：State（状态）、Getter（计算属性）、Mutation（同步修改）、Action（异步操作）
- 状态变更遵循单向数据流，保证数据可预测性

**在本项目中的应用（store/index.js）：**
```javascript
export default createStore({
  state: {
    user: initialUser,
    token: localStorage.getItem('token') || null,
    theme: localStorage.getItem('theme') || 'default',
    questions: [],
    filters: { /* 筛选条件 */ }
  },
  mutations: {
    // 同步修改状态
    SET_USER(state, user) {
      state.user = user
      if (user) {
        localStorage.setItem('user', JSON.stringify(user))  // 持久化
      }
    }
  },
  actions: {
    // 异步操作
    async login({ commit }, { token, user }) {
      commit('SET_TOKEN', token)
      commit('SET_USER', user)
    }
  },
  getters: {
    // 计算属性
    isAuthenticated: state => !!state.token,
    isAdmin: state => state.user?.role === 'ADMIN'
  }
})
```

### 2.2 UI 组件库层

#### **Element Plus 2.13.0** - Vue 3 企业级组件库

**技术原理：**
- 基于 Vue 3 Composition API 构建
- 提供开箱即用的高质量组件（表格、表单、对话框、抽屉等）
- 支持主题定制和国际化

**在本项目中的使用：**
- **el-card**：卡片容器，用于内容分组
- **el-menu**：导航菜单，支持水平/垂直布局
- **el-table**：数据表格，支持排序、筛选、分页
- **el-progress**：进度条组件，用于学习进度展示
- **el-tag**：标签，用于题目标签（科目、难度、章节）
- **el-dialog/el-drawer**：弹窗和抽屉组件

#### **@element-plus/icons-vue 2.3.2** - 官方图标库

**图标注册方式（main.js）：**
```javascript
import * as ElementPlusIconsVue from '@element-plus/icons-vue'
for (const [key, component] of Object.entries(ElementPlusIconsVue)) {
  app.component(key, component)  // 全局注册所有图标
}
```

### 2.3 数据可视化层

#### **ECharts 6.0.0** + **vue-echarts 8.0.1**

**技术原理：**
- ECharts 是基于 Canvas 的图表库，支持多种图表类型
- vue-echarts 是 ECharts 的 Vue 3 封装，支持响应式更新
- 按需导入优化打包体积

**在本项目中的应用（Home.vue）：**
```javascript
import { use } from 'echarts/core'
import { CanvasRenderer } from 'echarts/renderers'
import { LineChart } from 'echarts/charts'
import { TitleComponent, TooltipComponent, GridComponent } from 'echarts/components'

// 按需注册组件，减少打包体积
use([CanvasRenderer, LineChart, TitleComponent, TooltipComponent, GridComponent])

// 图表配置
const chartOption = ref({
  xAxis: { type: 'category', data: [] },
  yAxis: { type: 'value', min: 0, max: 100 },
  series: [{ name: '整体正确率', type: 'line', data: [] }]
})
```

### 2.4 HTTP 通信层

#### **Axios 1.13.2** - Promise based HTTP client

**技术原理：**
- 基于 Promise 的 HTTP 客户端，支持浏览器和 Node.js
- 提供请求/响应拦截器机制
- 支持请求取消、超时设置、自动 JSON 转换

**在本项目中的封装（utils/request.js）：**
```javascript
const request = axios.create({
  baseURL: '',
  timeout: 10000,
  headers: { 'Content-Type': 'application/json' }
})

// 请求拦截器 - 自动添加认证令牌
request.interceptors.request.use(config => {
  const token = localStorage.getItem('token')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

// 响应拦截器 - 统一错误处理
request.interceptors.response.use(
  response => {
    const { code, message, data } = response.data
    if (code !== 200) {
      ElMessage.error(message || '请求失败')
      if (response.status === 401) {
        router.push('/login')
      }
      return Promise.reject(new Error(message))
    }
    return { data, message }
  },
  error => {
    // 处理网络错误和超时
    if (error.code === 'ECONNABORTED') {
      ElMessage.error('请求超时，请稍后重试')
    }
    // ... 更多错误处理
  }
)
```

### 2.5 构建工具层

#### **Vue CLI 5.0.0** + **Webpack**

**技术原理：**
- Vue CLI 基于 webpack 进行项目构建
- 支持热模块替换（HMR），开发时实时更新
- 生产环境自动代码分割、压缩、Tree Shaking

**配置（vue.config.js）：**
```javascript
module.exports = {
  transpileDependencies: true,  // 转译所有 node_modules 中的依赖
  devServer: {
    port: 8081,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',  // 代理到后端
        changeOrigin: true,  // 改变请求源
        pathRewrite: { '^/api': '/api' }
      }
    }
  }
}
```

**代理原理：** 开发环境解决跨域问题，将 `/api` 开头的请求转发到后端服务器

---

## 三、核心功能模块技术分析

### 3.1 用户认证模块

#### **登录功能（Login.vue）**

**技术实现：**
1. **表单验证**：使用 Element Plus 的 el-form 组件
2. **状态持久化**：登录成功后将 token 和 user 信息存入 localStorage
3. **路由跳转**：登录成功后跳转到首页

**数据流：**
```
用户输入 → API请求(/api/auth/login) → 后端验证 →
返回token+userInfo → 存储到localStorage → 更新Vuex → 跳转首页
```

#### **注册功能（Register.vue）**

**技术实现：**
1. **表单验证规则**
```javascript
const validatePass = (rule, value, callback) => {
  if (value === '') {
    callback(new Error('请输入密码'))
  } else if (value.length < 6) {
    callback(new Error('密码长度不能少于6位'))
  } else {
    callback()
  }
}

const validateConfirmPass = (rule, value, callback) => {
  if (value !== registerForm.password) {
    callback(new Error('两次输入密码不一致'))
  } else {
    callback()
  }
}
```

2. **表单字段**
- 用户名（3-20字符）
- 邮箱（格式验证）
- 密码（≥6位）
- 确认密码（一致性校验）
- 考试日期（可选）
- 用户协议勾选

3. **注册流程**
```javascript
// 注册成功后跳转到登录页
const res = await register({
  username: registerForm.username,
  email: registerForm.email,
  password: registerForm.password
})
ElMessage.success('注册成功，请登录')
router.push('/login')
```

#### **权限控制**

**实现方式：**
1. **路由守卫**：在 router.beforeEach 中检查 token
2. **角色验证**：通过 meta.requiresAdmin 标记管理员路由
3. **组件级权限**：使用 Vuex getter 判断用户角色

```javascript
// Layout.vue 中的权限判断
const isAdmin = computed(() => store.getters.isAdmin)

<el-menu-item v-if="isAdmin" index="/question-bank">
  <!-- 仅管理员可见 -->
</el-menu-item>
```

### 3.2 模拟考试模块

#### **模拟考试列表（MockExam.vue）**

**技术实现：**

**1. 三个标签页**
- **历年真题**：表格展示历年试卷，支持年份筛选、开始考试、下载试卷
- **自定义模拟**：配置参数生成个性化试卷
- **最近记录**：查看历史考试记录，支持继续未完成考试

**2. 自定义组卷功能**
```javascript
const customSettings = ref({
  subject: '',      // 科目选择
  difficulty: '',   // 难度选择
  count: 40,        // 题目数量
  duration: 120,    // 考试时长
  types: ['SINGLE', 'MULTIPLE']  // 题型选择
})
```

**3. 考试记录管理**
- 正确率进度条（带颜色分级）
- 考试进度显示
- 状态标签（已完成/进行中）

#### **考试详情页（ExamDetail.vue）**

**技术亮点：**

**1. 考试头部组件**
```javascript
// 圆形进度计时器
<el-progress
  type="circle"
  :percentage="timeProgress"
  :width="100"
  :color="getTimerColor"  // 根据剩余时间变色
>
```

**2. 答题状态管理**
```javascript
// 动态答案对象（支持不同题型）
const answers = reactive({})

// 答题统计
const answeredCount = computed(() => {
  const count = Object.values(answers).filter(answer => {
    if (Array.isArray(answer)) {
      return answer.some(item => item !== null && item !== '')
    }
    return answer !== null && answer !== ''
  }).length
  return count
})
```

**3. 答题卡弹窗**
```javascript
// 网格布局显示答题状态
<div class="question-grid">
  <div
    v-for="(question, index) in questions"
    :key="question.id"
    class="grid-item"
    :class="{
      'answered': isAnswered(question.id),
      'current': index === currentQuestionIndex,
      'marked': question.marked
    }"
    @click="jumpToQuestion(index)"
  >
    {{ index + 1 }}
  </div>
</div>
```

**4. 答案格式转换**
```javascript
// 提交时统一转换答案格式
const formattedAnswers = {}
for (const [questionId, answer] of Object.entries(answers)) {
  if (Array.isArray(answer)) {
    // 多选题：[0,1,2] → "A,B,C"
    formattedAnswers[questionId] = answer
      .map(idx => String.fromCharCode(65 + idx))
      .join(',')
  } else if (typeof answer === 'number') {
    // 单选题：0 → "A"
    formattedAnswers[questionId] = String.fromCharCode(65 + answer)
  } else {
    formattedAnswers[questionId] = String(answer)
  }
}
```

#### **考试结果页（ExamResult.vue）**

**功能特性：**

**1. 分数展示**
```javascript
// 主分数显示
<div class="main-score">
  <span class="score-value">{{ score }}</span>
  <span class="score-total">/ {{ examInfo.totalScore }}</span>
</div>
<div class="score-percent">{{ scorePercent }}%</div>
```

**2. 统计数据**
- 已答题数
- 答对题数
- 答错题数
- 用时统计

**3. 各科目得分分析**
```javascript
<div v-for="subject in subjectScores" :key="subject.name">
  <el-progress
    :percentage="Math.round(subject.correct / subject.total * 100)"
    :color="getProgressColor(subject.correct / subject.total * 100)"
  />
</div>
```

**4. 学习建议**
- 优秀（≥120分）
- 良好（≥90分）
- 及格（≥60分）
- 需加强（<60分）

**5. 数据来源**
- 优先从 localStorage 读取（交卷时保存）
- 降级到后端 API 获取

---

### 3.3 练习模块

#### **练习模式选择（Practice.vue）**

**三种模式：**
1. **单题模式（single）**：逐题练习，立即反馈
2. **套题模式（batch）**：模拟考试，限时180分钟
3. **专项模式（special）**：按章节筛选练习

#### **练习模式核心（PracticeMode.vue）**

**技术亮点：**

**1. 会话管理机制**
```javascript
// 创建练习会话
const res = await createPracticeSession({
  mode: mode.value,
  subject: subject,
  chapter: chapter,
  difficulty: difficulty,
  count: count
})
sessionId.value = res.data.sessionId

// 更新进度（每答完一题）
await updateSessionProgress(sessionId.value, {
  currentIndex: currentIndex.value,
  userAnswer: answerText,
  isCorrect: answerResult.isCorrect
})

// 完成练习
await completeSession(sessionId.value)
```

**2. 计时器实现**
```javascript
// 单题模式计时器
const timer = ref(0)
timerInterval = setInterval(() => {
  timer.value++
}, 1000)

// 套题模式倒计时（3小时 = 10800秒）
const examTime = ref(10800)
timerInterval = setInterval(() => {
  examTime.value--
  if (examTime.value <= 0) {
    forceSubmit()  // 自动交卷
  }
}, 1000)
```

**3. 答案状态管理**
```javascript
// 使用响应式数组存储每题状态
const questionStatus = reactive([])  // { answered, isCorrect }
const userAnswers = reactive([])     // 用户答案

// 多选题深拷贝处理（避免引用问题）
if (currentQuestion.value?.type === 'MULTIPLE' && Array.isArray(userAnswers[index])) {
  userAnswer.value = [...userAnswers[index]]
}
```

**4. 题目类型适配**
```javascript
// 单选题 - el-radio-group
// 多选题 - el-checkbox-group
// 填空题 - el-input(type="textarea")
// 综合题 - el-input + 评分组件(el-rate)
```

**5. 进度恢复机制**
```javascript
// 通过 URL 参数恢复练习
if (route.query.continue) {
  const res = await getSessionDetail(route.query.continue)
  // 恢复题目和已答状态
  questions.value = res.data.questions.map(q => parseQuestionData(q))
}
```

### 3.4 错题本模块（Mistakes.vue）

**功能特性：**

**1. 顶部统计卡片**
- 总错题数、已复习、今日新增、待复习
- 渐变色图标背景
- 实时统计计算

**2. 复习模式筛选**
```javascript
// 艾宾浩斯遗忘曲线实现
if (reviewMode.value === 'ebbinghaus') {
  const reviewPeriods = [1, 2, 4, 7, 15]  // 复习周期：1天、2天、4天、7天、15天
  filtered = filtered.filter(mistake => {
    const nextReviewDay = reviewPeriods[mistake.reviewCount]
    return daysDiff >= nextReviewDay
  })
}
```

**3. 双视图模式**
- **列表视图**：表格展示，支持选择、排序
- **卡片视图**：卡片网格布局，更直观

**4. 错题详情弹窗**
- 题目内容和选项
- 用户错误答案 vs 正确答案对比
- 题目解析展示
- 个人笔记编辑（带图片上传）
- 相关考点标签（可点击跳转）

**5. 批量操作**
- 导出错题（Excel 格式）
- 清除已复习错题
- 批量删除选中错题

**6. API 接口**
```javascript
// src/api/mistake.js
export const getWrongQuestions = (params)    // 获取错题列表
export const markMastered = (id)             // 标记已掌握
export const deleteWrongQuestion = (id)      // 删除单个错题
export const batchDeleteWrongQuestions = (ids) // 批量删除
export const exportWrongQuestions = (params)    // 导出 Excel
export const getReviewQuestions = (params)      // 艾宾浩斯待复习
export const setWrongReason = (id, reason)      // 设置错误原因
```

---

### 3.5 学习笔记模块（Notes.vue）

**技术实现：**

**1. 左右分栏布局**
```javascript
<el-row :gutter="20">
  <el-col :span="8">  // 左侧笔记列表
  <el-col :span="16"> // 右侧编辑区
</el-row>
```

**2. 笔记列表功能**
- 搜索框（实时过滤标题和内容）
- 分类筛选（按科目）
- 时间排序显示
- 预览文本截断

**3. 笔记编辑功能**
- 标题编辑
- 分类选择
- 内容自动保存（防抖 1 秒）
- 手动保存按钮
- 删除笔记（带确认）

**4. 数据存储**
```javascript
// 使用 localStorage 持久化
const loadNotes = () => {
  const savedNotes = localStorage.getItem('userNotes')
  if (savedNotes) {
    notes.value = JSON.parse(savedNotes)
  }
}

const saveNotes = () => {
  localStorage.setItem('userNotes', JSON.stringify(notes.value))
}
```

**5. 防抖保存**
```javascript
// 内容变化时延迟保存
const handleContentChange = () => {
  clearTimeout(saveTimer.value)
  saveTimer.value = setTimeout(() => {
    saveCurrentNote()
  }, 1000)
}
```

**6. 关联题目**
- 笔记可关联多道题目
- 点击可跳转到对应题目

---

### 3.6 个人中心模块（Profile.vue）

**功能模块：**

**1. 个人信息卡片**
- 头像显示
- 用户名、角色徽章
- 邮箱、注册时间、考试日期
- 编辑资料、修改密码按钮

**2. 资料编辑对话框**
```javascript
// 表单验证规则
const editRules = {
  username: [
    { required: true, message: '请输入用户名', trigger: 'blur' },
    { min: 2, max: 20, message: '用户名长度在 2 到 20 个字符', trigger: 'blur' }
  ],
  email: [
    { required: true, message: '请输入邮箱', trigger: 'blur' },
    { type: 'email', message: '请输入正确的邮箱格式', trigger: 'blur' }
  ]
}
```

**3. 修改密码功能**
- 原密码验证
- 新密码强度检查（至少 6 位）
- 确认密码一致性验证
- 修改成功后自动退出登录

**4. 学习计划设置**
- 每日刷题目标（数字输入）
- 考试日期选择器
- 保存到本地

**5. 今日任务管理**
```javascript
// 任务数据结构
{
  id: Date.now(),
  text: '任务内容',
  completed: false
}

// 任务操作
- 添加任务（回车快捷键）
- 切换完成状态
- 删除任务
- 完成进度条
```

**6. 学习统计展示**
- 总刷题量
- 平均正确率
- 各科目学习进度（柱状图）
- 薄弱环节分析（可点击针对性练习）
- 学习时长统计（今日/本周/累计）

**7. 薄弱环节表格**
```javascript
<el-table :data="weaknessData">
  <el-table-column prop="chapter" label="章节" />
  <el-table-column prop="subject" label="科目" />
  <el-table-column prop="errorRate" label="错误率">
    <el-tag :type="errorRate > 0.5 ? 'danger' : errorRate > 0.3 ? 'warning' : 'success'">
  </el-table-column>
  <el-table-column label="操作">
    <el-button @click="practiceWeakness(row)">针对练习</el-button>
  </el-table-column>
</el-table>
```

---

### 3.7 数据导入模块（Import.vue）

**技术实现：**

**1. 两种导入方式**
- **文件上传**：支持拖拽上传
- **文本粘贴**：直接粘贴题目文本

**2. 文件上传组件**
```javascript
<el-upload
  drag
  :auto-upload="false"
  :limit="1"
  :on-change="handleFileChange"
  accept=".txt,.md,.docx,.pdf"
>
```

**3. 文本解析预览**
- 解析状态摘要（总题数、成功解析、警告数量）
- 题目列表表格预览
- 置信度显示（进度条）
- 题目编辑功能

**4. 解析结果数据结构**
```javascript
{
  importId: '...',           // 导入批次ID
  overallConfidence: 0.85,   // 整体置信度
  totalCount: 10,           // 题目总数
  successCount: 8,          // 成功解析
  warningCount: 2,          // 警告数量
  questions: [
    {
      topic: '题目内容',
      type: 'SINGLE',
      difficulty: 'EASY',
      confidence: 0.9,      // 单题置信度
      warning: false,
      // ...
    }
  ]
}
```

**5. 编辑题目功能**
- 修改题目内容
- 调整题型、难度、年份
- 修改答案和解析
- 保存后更新预览

**6. 确认导入流程**
```javascript
// 1. 解析文件/文本 → parsedResult
// 2. 编辑预览题目 → 更新 parsedResult
// 3. 确认导入 → 调用 API
const confirmImport = async () => {
  const res = await confirmImportApi(parsedResult.value.importId)
  if (res.code === 0) {
    ElMessage.success('导入成功')
    parsedResult.value = null
  }
}
```

---

### 3.8 数据可视化模块

#### **首页统计（Home.vue）**

**技术实现：**

**1. 统计卡片展示**
- 总题数、已完成、正确率、备考天数
- 渐变色图标背景
- 悬停动画效果

**2. 学习进度条**
```javascript
<el-progress
  :percentage="subject.percentage"
  :color="subject.color"
/>
```

**3. 正确率趋势图**
- 使用 ECharts 折线图
- 支持 7/30/90 天趋势
- 动态数据加载

**数据获取流程：**
```javascript
// 调用 API 获取统计数据
const userStatsRes = await getUserStatistics()
const subjectStatsRes = await getSubjectStatistics()
const trendRes = await getCorrectRateTrend(7)

// 数据转换和渲染
stats.value = { /* ... */ }
subjects.value = subjectStatsRes.data.map(item => ({ /* ... */ }))
chartOption.value.series = [{ /* ... */ }]
```

### 3.4 主题系统

#### **双主题切换（ThemeSwitcher.vue + theme.css）**

**技术原理：**

**1. CSS 变量定义**
```css
:root, [data-theme="default"] {
  --bg-color: #f5f7fa;
  --text-color-primary: #303133;
  --primary-color: #409EFF;
}

[data-theme="eye-care"] {
  --bg-color: #FDF6E3;      /* 杏色背景 */
  --text-color-primary: #5D4E37;
  --primary-color: #CD853F;  /* 柔和杏色 */
}
```

**2. 主题切换实现**
```javascript
const toggleTheme = () => {
  const newTheme = isEyeCareMode.value ? 'default' : 'eye-care'
  store.dispatch('setTheme', newTheme)
  document.documentElement.setAttribute('data-theme', newTheme)
  localStorage.setItem('theme', newTheme)  // 持久化
}
```

**3. 护眼模式特殊效果**
```css
[data-theme="eye-care"] {
  filter: contrast(0.95) saturate(0.9);  /* 降低对比度和饱和度 */
}
```

### 3.5 布局系统

#### **主布局（Layout.vue）**

**布局结构：**
```
el-container
├── el-header (顶部导航栏)
│   ├── logo
│   ├── el-menu (导航菜单)
│   └── header-right (主题切换 + 用户下拉菜单)
└── el-main (主内容区)
    └── router-view
```

**技术亮点：**

**1. 响应式导航**
```javascript
const activeMenu = computed(() => route.path)  // 当前激活菜单高亮
```

**2. 下拉菜单**
```javascript
<el-dropdown @command="handleCommand">
  <span class="user-info">{{ user?.username }}</span>
  <template #dropdown>
    <el-dropdown-menu>
      <el-dropdown-item command="profile">个人中心</el-dropdown-item>
      <el-dropdown-item command="logout">退出登录</el-dropdown-item>
    </el-dropdown-menu>
  </template>
</el-dropdown>
```

**3. 渐变装饰条**
```css
.top-header::after {
  content: '';
  background: linear-gradient(90deg, #409EFF 0%, #67C23A 50%, #E6A23C 100%);
}
```

---

### 3.6 题库管理模块（QuestionBank.vue）- 管理员

**功能特性：**

**1. 多维度筛选**
- 科目选择（联动加载章节）
- 章节、题型、难度、年份筛选
- 搜索和重置功能

**2. 双视图模式**
- **列表视图**：表格展示，支持批量选择
- **卡片视图**：卡片网格布局，更直观

**3. 题目 CRUD 操作**
```javascript
// 添加/编辑题目
const questionForm = reactive({
  subject: '',      // 科目
  chapter: '',      // 章节
  type: 'SINGLE',   // 题型
  difficulty: 'EASY', // 难度
  year: null,       // 年份
  topic: '',        // 题干
  optionsList: ['', ''], // 选项列表
  answer: '',       // 答案
  analysis: '',     // 解析
  tags: ''          // 考点标签
})
```

**4. 批量导入功能**
```javascript
// 文件上传解析
const submitImport = async () => {
  const formData = new FormData()
  formData.append('file', fileList.value[0].raw)
  if (filters.subject) formData.append('subject', filters.subject)
  const res = await uploadQuestions(formData)
  importId.value = res.data.importId
  importPreviewList.value = res.data.previewQuestions
}

// 确认导入
await confirmImport(importId.value)
```

**5. 导出功能**
```javascript
// 创建 Excel 下载
const blob = new Blob([res], {
  type: 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'
})
const url = window.URL.createObjectURL(blob)
const link = document.createElement('a')
link.href = url
link.download = `题目库_${new Date().toLocaleDateString()}.xlsx`
link.click()
```

---

### 3.7 收藏模块（Collections.vue）

**功能特性：**

**1. 分类管理**
```javascript
// 收藏分类 Tabs
<el-tabs v-model="activeFolder">
  <el-tab-pane label="全部收藏" name="all">
    <!-- 全部收藏题目 -->
  </el-tab-pane>
  <el-tab-pane v-for="folder in folders" :key="folder.id" :name="String(folder.id)">
    <!-- 按分类显示 -->
  </el-tab-pane>
</el-tabs>
```

**2. 新建分类对话框**
```javascript
const folderForm = reactive({
  name: '',           // 分类名称
  description: '',    // 描述
  color: '#409EFF'    // 颜色选择器
})
```

**3. 批量操作**
- 批量取消收藏
- 批量移动到分类
- 导出收藏（开发中）

**4. 题目详情弹窗**
- 完整题目内容
- 答案和解析
- 收藏笔记
- 关联练习跳转

**5. 移动到分类功能**
```javascript
// 单个或批量移动
const confirmMove = async () => {
  await Promise.all(
    moveQuestions.value.map(q => moveQuestionToFolder(q.questionId, selectedFolderId.value))
  )
  ElMessage.success('移动成功')
}
```

---

### 3.8 任务管理模块（Tasks.vue）

**功能特性：**

**1. 任务统计卡片**
```javascript
const stats = reactive({
  total: 0,      // 全部任务
  completed: 0,  // 已完成
  pending: 0,    // 进行中
  overdue: 0     // 已逾期
})
```

**2. 任务分类**
- 日常练习
- 专项突破
- 模拟考试
- 复习计划

**3. 任务优先级**
- 高（3级）- danger 红色
- 中（2级）- warning 橙色
- 低（1级）- info 灰色

**4. 任务状态切换**
```javascript
// 复选框切换完成状态
const toggleComplete = async (task) => {
  const res = await toggleTaskComplete(task.id, task.completed)
  if (res.code === 0) {
    ElMessage.success(task.completed ? '任务已完成' : '任务已恢复')
    loadStats()
  }
}
```

**5. 逾期判断**
```javascript
const isOverdue = (targetDate) => {
  if (!targetDate) return false
  return new Date(targetDate) < new Date()
}
// 逾期任务显示红色
```

---

### 3.9 考试入口模块（Exam.vue）

**功能特性：**

**1. 两种考试模式**
- **历年真题**：选择年份真题
- **自定义模拟**：灵活组卷

**2. 历年真题选择器**
```javascript
// 按年份选择
<el-tab-pane label="按年份选择" name="year">
  <el-card v-for="exam in pastExamsByYear" @click="selectPastExam(exam)">
    <h4>{{ exam.year }}年真题</h4>
    <p>{{ exam.totalQuestions }}题 | {{ exam.totalScore }}分</p>
    <span>平均分: {{ exam.averageScore }}</span>
    <span>难度: {{ exam.difficulty }}</span>
  </el-card>
</el-tab-pane>

// 按科目选择
<el-tab-pane label="按科目选择" name="subject">
  <!-- 科目筛选表格 -->
</el-tab-pane>
```

**3. 自定义模拟卷配置**
```javascript
const customExamForm = reactive({
  name: '',                    // 试卷名称
  duration: 180,               // 考试时长
  subjects: ['ds', 'cs', 'os', 'cn'], // 科目组合
  questionCount: 40,           // 题目数量
  difficulty: {
    easy: 30, medium: 50, hard: 20  // 难度分布
  },
  questionTypes: {
    single: 40, multiple: 20, fill: 20, comprehensive: 20  // 题型比例
  }
})
```

**4. 最近考试记录**
- 考试类型标签
- 得分颜色分级（绿/橙/红）
- 正确率圆形进度条
- 用时和排名显示
- 查看报告/复习错题操作

**5. 时间格式化**
```javascript
const formatTime = (seconds) => {
  const hours = Math.floor(seconds / 3600)
  const minutes = Math.floor((seconds % 3600) / 60)
  return `${hours}h${minutes}m`
}
```

---

### 3.10 试卷复习模块（ExamReview.vue）

**功能特性：**

**1. 答题状态对比**
```javascript
// 用户答案状态标签
const getUserAnswerStatus = (question) => {
  const userAns = userAnswers.value[question.id]
  if (!userAns) return 'info'
  return userAns.trim().toUpperCase() === question.answer.trim().toUpperCase()
    ? 'success'  // 正确-绿色
    : 'danger'   // 错误-红色
}
```

**2. 选项高亮显示**
```javascript
// 正确答案高亮
:class="{ 'correct-option': isCorrectOption(question.answer, idx) }"

// 用户选择高亮
:class="{ 'user-option': isUserOption(question.id, idx) }"

// 既正确又选择（绿色背景）
.user-option.correct-option {
  background-color: #f0f9ff;
  border-color: #67c23a;
}
```

**3. 答案解析展示**
- 正确答案 vs 用户答案对比
- 详细解析内容
- 题目类型标签

**4. 数据加载**
```javascript
// 从会话详情加载
const loadReviewData = async () => {
  const res = await getSessionDetail(sessionId)
  questions.value = res.data.questions || []
  userAnswers.value = res.data.userAnswers || {}
}
```

---

## 四、数据交互架构

### 4.1 API 模块化设计

**目录结构：**
```
src/api/
├── auth.js          # 认证相关接口
├── practice.js      # 练习和题目相关接口
├── mistake.js       # 错题相关接口
├── notes.js         # 笔记相关接口
├── collection.js    # 收藏相关接口
├── tasks.js         # 任务相关接口
├── statistics.js    # 统计相关接口
├── answer.js        # 答案相关接口
├── import.js        # 导入相关接口
└── knowledge.js     # 知识点相关接口
```

**主要 API 模块功能：**

**1. auth.js - 认证模块**
```javascript
register(data)              // 用户注册
login(data)                 // 用户登录
getUserInfo()               // 获取用户信息
updateProfile(data)         // 更新用户资料
changePassword(data)        // 修改密码
```

**2. practice.js - 练习模块**
```javascript
getQuestions(params)        // 获取题目列表（分页+筛选）
getQuestion(id)             // 获取单个题目
getRandomQuestion(params)   // 获取随机题目
getChapters(subject)        // 获取章节列表
addQuestion(data)           // 新增题目（管理员）
updateQuestion(data)        // 更新题目（管理员）
deleteQuestion(id)          // 删除题目（管理员）
createPracticeSession(data) // 创建练习会话
getRecentSessions(limit)    // 获取最近练习记录
getSessionDetail(sessionId) // 获取会话详情（恢复练习）
updateSessionProgress(id, data) // 更新会话进度
completeSession(id)         // 完成练习会话
getExamPapers(params)       // 获取试卷列表
getExamPaperDetail(id)      // 获取试卷详情
startExam(id)               // 开始考试
createCustomExam(data)      // 创建自定义模拟考试
getExamRecords(limit)       // 获取考试记录列表
getExamRecordDetail(id)     // 获取考试详情
submitExamResult(id, data)  // 提交考试结果
```

**3. mistake.js - 错题模块**
```javascript
getWrongQuestions(params)      // 获取错题列表（分页）
addNote(id, note)              // 添加笔记
markMastered(id)               // 标记已掌握
deleteWrongQuestion(id)        // 删除单个错题
batchDeleteWrongQuestions(ids) // 批量删除错题
exportWrongQuestions(params)   // 导出错题（Excel）
getWrongQuestionStats()        // 获取错题统计
getReviewQuestions(params)    // 获取待复习错题（艾宾浩斯）
updateReviewStatus(id, remembered) // 更新复习状态
setWrongReason(id, reason)     // 设置错误原因
```

**4. statistics.js - 统计模块**
```javascript
getUserStatistics()           // 获取用户统计数据
getSubjectStatistics()        // 获取各科目统计
getCorrectRateTrend(days)     // 获取正确率趋势
getWeakChapters()             // 获取薄弱章节
getHeatMap(params)            // 获取学习热力图数据
getDailyDetail(params)        // 获取每日学习详情
generateReport(params)        // 生成学习报告
```

**5. import.js - 导入模块**
```javascript
uploadFile(file, subject)     // 上传文件解析
parseText({ text })            // 解析文本
confirmImport(importId)        // 确认导入
cancelImport(importId)         // 取消导入
updatePreviewQuestion(importId, index, data) // 更新预览题目
```

**API 调用示例：**
```javascript
// api/practice.js
export const createPracticeSession = (data) => {
  return request({
    url: '/api/practice/session',
    method: 'post',
    data
  })
}

// 组件中使用
import { createPracticeSession } from '@/api/practice'
const res = await createPracticeSession({ mode, subject, count })
```

### 4.2 请求/响应流程

```
组件 → API模块 → request实例 → 请求拦截器(添加token) →
后端API → 响应拦截器(统一处理) → 组件更新
```

### 4.3 错误处理策略

**统一错误处理（utils/request.js）：**
```javascript
// 网络错误
if (error.code === 'ECONNABORTED') {
  ElMessage.error('请求超时，请稍后重试')
}

// HTTP 状态码处理
switch (status) {
  case 401: ElMessage.error('未授权，请重新登录'); break
  case 403: ElMessage.error('拒绝访问'); break
  case 404: ElMessage.error('请求的资源不存在'); break
  case 500: ElMessage.error('服务器内部错误'); break
}
```

---

## 五、性能优化策略

### 5.1 代码分割

**路由懒加载：**
```javascript
component: () => import('@/views/PracticeMode.vue')
```

**原理：** Webpack 将每个路由组件打包成单独的 chunk，按需加载

### 5.2 按需导入

**ECharts 按需导入：**
```javascript
import { LineChart } from 'echarts/charts'
import { CanvasRenderer } from 'echarts/renderers'
use([CanvasRenderer, LineChart])  // 只导入需要的组件
```

### 5.3 响应式优化

**计算属性缓存：**
```javascript
const currentQuestion = computed(() => questions.value[currentIndex.value])
// 仅当依赖变化时重新计算
```

### 5.4 虚拟滚动

**题目列表：** 使用 el-drawer 显示，避免大量 DOM 渲染

---

## 六、项目特色技术

### 6.1 艾宾浩斯记忆曲线

**错题复习算法：** 根据遗忘曲线安排复习时间点（1天、3天、7天、15天、30天）

### 6.2 学习数据持久化

**多重存储策略：**
1. **localStorage**：用户信息和 token
2. **Vuex + localStorage**：题目筛选条件、主题设置
3. **服务端会话**：练习进度、答题记录

### 6.3 题目解析动态渲染

**富文本内容支持：**
```vue
<div v-html="currentQuestion.content"></div>
<div v-html="currentQuestion.explanation"></div>
```

**注意：** 生产环境需配合 XSS 防护措施

---

## 七、技术难点与解决方案

### 7.1 多选题答案状态管理

**问题：** 多选题用户答案是数组，直接赋值会导致所有题目共享同一数组

**解决方案：**
```javascript
// 深拷贝数组
if (currentQuestion.value?.type === 'MULTIPLE' && Array.isArray(userAnswers[index])) {
  userAnswer.value = [...userAnswers[index]]
}
```

### 7.2 倒计时精度问题

**问题：** setInterval 长期运行会产生时间漂移

**解决方案：** 基于时间戳计算差值
```javascript
const startTime = Date.now()
const expectedTime = 10800 * 1000
timerInterval = setInterval(() => {
  const elapsed = Date.now() - startTime
  examTime.value = Math.max(0, expectedTime - elapsed) / 1000
}, 1000)
```

### 7.3 跨域问题

**开发环境：** Vue CLI 代理转发
```javascript
proxy: {
  '/api': {
    target: 'http://localhost:8080',
    changeOrigin: true
  }
}
```

**生产环境：** 后端配置 CORS 或使用 Nginx 反向代理

---

## 八、项目目录结构

```
src/
├── api/                # API接口
│   ├── auth.js         # 认证相关接口
│   ├── practice.js     # 练习相关接口
│   ├── mistake.js      # 错题相关接口
│   ├── notes.js        # 笔记相关接口
│   ├── collection.js   # 收藏相关接口
│   ├── tasks.js        # 任务相关接口
│   ├── statistics.js   # 统计相关接口
│   ├── answer.js       # 答案相关接口
│   ├── import.js       # 导入相关接口
│   └── knowledge.js    # 知识点相关接口
├── assets/             # 静态资源
├── components/         # 组件
│   └── common/         # 通用组件
│       └── Layout.vue  # 布局组件
├── router/             # 路由配置
│   └── index.js        # 路由定义及守卫
├── store/              # Vuex状态管理
│   └── index.js
├── utils/              # 工具函数
├── views/              # 页面组件
│   ├── Login.vue       # 登录页
│   ├── Register.vue    # 注册页
│   ├── Home.vue        # 首页
│   ├── Profile.vue     # 个人中心
│   ├── Practice.vue    # 练习页
│   ├── PracticeMode.vue # 练习模式页
│   ├── Mistakes.vue    # 错题本
│   ├── Notes.vue       # 笔记
│   ├── Collections.vue # 收藏
│   ├── Tasks.vue       # 任务
│   ├── MockExam.vue    # 模拟考试列表
│   ├── Exam.vue        # 考试页
│   ├── ExamDetail.vue  # 考试详情页
│   ├── ExamResult.vue  # 考试结果页
│   ├── ExamReview.vue  # 试卷复习页
│   ├── Import.vue      # 导入页
│   └── QuestionBank.vue # 题库管理（管理员）
├── App.vue             # 根组件
└── main.js             # 入口文件
```

---

## 九、总结

这是一个**企业级 Vue 3 单页应用**，采用了现代化的前端技术栈：

| 技术领域 | 技术选型 | 核心优势 |
|---------|---------|---------|
| 框架层 | Vue 3 + Composition API | 更好的逻辑复用和 TypeScript 支持 |
| 路由 | Vue Router 4 | 官方路由方案，功能完善 |
| 状态管理 | Vuex 4 | 集中式状态管理，数据流可预测 |
| UI 组件 | Element Plus | 企业级组件库，开箱即用 |
| 数据可视化 | ECharts | 功能强大的图表库 |
| HTTP 客户端 | Axios | Promise 支持，拦截器机制 |
| 构建工具 | Vue CLI + Webpack | 成熟的开发构建方案 |

### 核心功能模块概览

| 模块 | 功能 | 技术亮点 |
|-----|------|---------|
| 用户认证 | 登录/注册/权限控制 | 路由守卫、角色验证 |
| 模拟考试 | 历年真题/自定义组卷/考试记录 | 计时器、答题卡、状态管理 |
| 练习模式 | 单题/套题/专项练习 | 会话管理、进度恢复 |
| 错题本 | 错题统计/艾宾浩斯复习/导出 | 遗忘曲线算法、批量操作 |
| 学习笔记 | 分类管理/搜索/自动保存 | 防抖保存、本地存储 |
| 个人中心 | 资料管理/任务管理/数据分析 | 表单验证、图表展示 |
| 数据导入 | 文件上传/文本粘贴/解析预览 | 拖拽上传、置信度显示 |
| 数据统计 | 学习统计/趋势分析/薄弱环节 | ECharts 图表、动态数据 |
| 主题系统 | 默认/护眼模式切换 | CSS 变量、本地持久化 |

### 项目特色

1. **完整的学习闭环**：练习 → 考试 → 错题 → 复习 → 笔记 → 数据分析

2. **智能化学习辅助**：
   - 艾宾浩斯记忆曲线复习提醒
   - 薄弱环节自动分析
   - 个性化学习建议

3. **丰富的数据可视化**：
   - 正确率趋势图
   - 各科目学习进度
   - 学习时长统计
   - 考试结果分析

4. **完善的用户体验**：
   - 双主题切换（默认/护眼）
   - 会话断点续练
   - 实时自动保存
   - 响应式布局

5. **企业级代码规范**：
   - 模块化 API 设计
   - 统一的错误处理
   - 请求/响应拦截
   - 权限控制完善

+9-+
